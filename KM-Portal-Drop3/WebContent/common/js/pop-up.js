$(document).ready(function() {
	$(".overlay-trigger").overlay({
		absolute: true,
		close: 'a.close-button1',
		closeOnClick: true,
		expose: { 
		        color: '#000000', 
		        loadSpeed: 200, 
		        opacity: 0.7 
		    },
		top: 'center',
		
		left: 'center'
	});
	
	
});
